{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Serve",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4825/",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "serve-scratch-gpt"
    }
  ]
}
